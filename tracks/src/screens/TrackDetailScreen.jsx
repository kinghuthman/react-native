import React from "react";
import { Text, View, StyleSheet } from "react-native";

const TrackDetail = ({ params }) => (
  <View>
    <Text>TrackDetail</Text>
  </View>
);

const styles = StyleSheet.create({});
export default TrackDetail;
